@model DBFirstEF_LMS.Models.Student
@{
    ViewBag.Title = "Show student classes";


}
<h2>Student records for @Html.DisplayFor(model => model.StudentID)</h2>
<h3>Menu</h3>
<div>
    <table>          
            @foreach (var l in ViewBag.ViewList)
            {
                <tr>
                    <td>
                        @l
                    </td>
                </tr>
            }
        <tr>
            <td>
                here too
            </td>
        </tr>
    </table>
</div>




@*looks good from here
    <h2>
        Courses for @Html.DisplayFor(model => model.Fname) @Html.DisplayFor(model => model.Lname)
    </h2>

    @foreach (var secn in ViewBag.Sections)
    {
        <tr>
            <td>
                @secn.section_id
            </td>
        </tr>

    }
    @foreach (var course in ViewBag.Courses)
    {
       <tr>
        <td>
            @course.course_id
        </td>
           <td>
               @course.course_name
           </td>
           <br />
           <td>
               @course.course_desc
           </td>
       </tr>
    }
    end looks good*@



@*<table>
        <tr>
            <td>
                <table>
                    <thead>
                        <tr>
                            <td>column 1 header 1</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var secn in ViewBag.Sections)
                        {
                            <tr>
                                <td>
                                    @secn.day_of_week
                                </td>
                            </tr>


                        }
                    </tbody>
                </table>
            </td>
            <td>
                <table>
                    <thead>
                        <tr>
                            <td>column 2 header 1</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var course in ViewBag.Courses)
                        {
                            <tr>
                                <td>
                                    @course.course_id
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </td>
            <td>
                <table>
                    <thead>
                        <tr>
                            <td>column 3 header 1</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var course in ViewBag.Courses)
                        {
                            <tr>
                                <td>
                                    @course.course_name
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </td>
            <td>
                <table>
                    <thead>
                        <tr>
                            <td>column 4 header 1</td>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>
                                <a href="@Url.Action("Action","Controller")"> <input type="button" value="Class Details" /> </a>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </td>
        </tr>
    </table>*@




